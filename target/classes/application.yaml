spring:
  application:
    name: blogapp
  datasource:
    url: jdbc:postgresql://localhost:5432/BlogPostApp
    driver: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


